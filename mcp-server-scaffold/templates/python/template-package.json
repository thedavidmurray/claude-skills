{
  "name": "{{SERVER_NAME}}",
  "version": "{{VERSION}}",
  "description": "{{DESCRIPTION}}",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "{{SERVER_NAME}}": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "{{SERVER_NAME}}"
  ],
  "author": "{{AUTHOR}}",
  "license": "{{LICENSE}}",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"{{EXTRA_DEPS}}
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"{{EXTRA_DEV_DEPS}}
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
